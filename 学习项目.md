```ini
export default { name:'jack', height: 170, age:23 } 

import 接收名称 from '模块名称'

而我们在index中使用
export { default as useSafeState } from './useSafeState'
export { default as useBooleanState } from './useBooleanState'

就可以在业务中按需引入，不用一个默认导出一个移入了！
```

错误边界、路由问题

react-store问题

